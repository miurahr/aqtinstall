version: 2

sphinx:
  configuration: docs/conf.py

formats:
  - epub
  - pdf

python:
   version: 3.7
   install:
      - method: setuptools
        path: .
      - method: pip
        path: .
        extra_requirements:
           - docs
   system_packages: false
